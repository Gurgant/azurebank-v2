meta {
  name: Set PIN
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/pin
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "pin": "123456"
  }
}

assert {
  res.status: eq 200
}

tests {
  test("Status is 200 OK", function() {
    expect(res.status).to.equal(200);
  });

  test("Success message returned", function() {
    expect(res.body.message).to.be.a('string');
  });
}
